import React from 'react';
import AlertDiv from '../../components/AlertDiv';

const Auth = () => {
  console.log('hello from auth');
  return (
    <div>
      <h1>Login page</h1>
      <AlertDiv />
    </div>
  );
};

// Auth.propTypes = {

// }

export default Auth;
